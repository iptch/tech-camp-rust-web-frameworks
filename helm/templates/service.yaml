apiVersion: v1
kind: Service
metadata:
  name: {{ include "textserver.fullname" . }}
  labels:
    {{- include "textserver.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "textserver.selectorLabels" . | nindent 4 }}
